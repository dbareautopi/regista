# STORY-012 — developer — 20260505T110000

## Resultado
❌ Fallo parcial — tests no compilan, no se avanza a In Review.

## Estado del código fuente
La implementación async ya está completada (sesiones anteriores, sin cambios nuevos en esta sesión).

- `cargo build` → ✅ limpio
- `cargo clippy -- -D warnings` → ✅ limpio
- `cargo fmt --check` → ✅ limpio

## Estado de los CAs

| CA | Estado | Notas |
|----|--------|-------|
| CA1 | ✅ | `process_story()` es `async fn`, invoca `invoke_with_retry(...).await` |
| CA2 | ✅ | `run_real()` usa `process_story(...).await` secuencialmente (sin `tokio::spawn`) |
| CA3 | ✅ | `run_dry()` es síncrono, no necesita tokio runtime |
| CA4 | ✅ | `run_hook()` usa `tokio::process::Command` internamente; se llama vía `spawn_blocking` desde async |
| CA5 | ✅ | `snapshot()`/`rollback()` en `git.rs` se llaman vía `spawn_blocking` desde async |
| CA6 | ❌ | `cargo test` no compila — 8 tests síncronos llaman funciones async sin `.await` |
| CA7 | ✅ | `cargo build` compila sin warnings |
| CA8 | ❌ | Bloqueado por CA6 |

## Errores de compilación en tests (NO corregidos — trabajo del QA)

8 errores E0599 en `src/app/pipeline.rs`:

| # | Test | Línea | Función llamada | Problema |
|---|------|-------|-----------------|----------|
| 1 | `story011::process_story_accepts_shared_state` | 1887 | `process_story(...)` | `#[test]` síncrono, falta `.await` |
| 2 | `story011::process_story_blocked_returns_early` | 1909 | `process_story(...)` | `#[test]` síncrono, falta `.await` |
| 3 | `story011::process_story_failed_returns_early` | 1930 | `process_story(...)` | `#[test]` síncrono, falta `.await` |
| 4 | `story012::run_real_with_terminal_stories_completes_in_one_iteration` | 2593 | `run_real(...)` | `#[test]` síncrono, falta `.await` |
| 5 | `story012::run_real_with_no_stories_completes_immediately` | 2633 | `run_real(...)` | `#[test]` síncrono, falta `.await` |
| 6 | `story012::run_real_with_draft_story_invokes_po_path` | 2680 | `result.is_ok()` | `#[test]` síncrono, falta `.await` |
| 7 | `story012::run_real_with_draft_story_invokes_po_path` | 2683 | `result.unwrap()` | `#[test]` síncrono, falta `.await` |
| 8 | `story012::run_real_shared_state_reflects_sequential_processing` | 2712 | `run_real(...)` | `#[test]` síncrono, falta `.await` |

**Causa raíz**: `process_story()` y `run_real()` son `async fn` que retornan `impl Future<...>`. Los tests usan `#[test]` (síncrono, sin runtime de tokio) y no aplican `.await`. Para que compilen, necesitan:
- Cambiar `#[test]` → `#[tokio::test]`
- Añadir `.await` en cada punto de llamada

**No se han corregido**: son errores en código de tests, no en código fuente. Corregirlos es responsabilidad del QA Engineer (transición Tests Ready → Tests Ready, fix). El orquestador debe pasar el turno al QA automáticamente.

## Verificaciones

- `cargo build` → ✅ 0 errores, 0 warnings
- `cargo clippy -- -D warnings` → ✅ 0 warnings
- `cargo fmt --check` → ✅ formato correcto
- `cargo test` → ❌ 8 errores de compilación (todos en tests)
