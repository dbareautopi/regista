# STORY-010 — qa-engineer — 20260505T012042

## Resultado
✅ Éxito


### Intento 1 (exit code: 0)

```

```

### stdout (intento 1)

```
Queda verificado. El archivo está intacto. Aquí está el resumen de los tests escritos:

---

## Tests para STORY-010 — Resumen

**Archivo**: `src/infra/agent.rs` (módulo `#[cfg(test)] mod tests`)

### Tests existentes conservados (funciones puras, no necesitan tokio)
| # | Test | Tipo |
|---|------|------|
| 1 | `build_feedback_prompt_includes_error` | `#[test]` — sin cambios |
| 2 | `build_feedback_prompt_truncates_long_output` | `#[test]` — sin cambios |
| 3 | `agent_options_default` | `#[test]` — sin cambios |

### Tests nuevos por cada CA

| CA | Test(s) | Qué valida |
|----|---------|------------|
| **CA1** | `invoke_once_is_async` | `#[tokio::test] async` — llama a `invoke_once().await`, demuestra que es async |
| **CA2** | `invoke_once_respects_timeout` | `#[tokio::test] async` — timeout de 100ms, verifica que `elapsed < 5s` (no busy-polling) |
| **CA3** | `invoke_with_retry_is_async` | `#[tokio::test] async` — llama a `invoke_with_retry().await`, demuestra que es async |
| **CA4** | `invoke_with_retry_preserves_retry_count` | Con `max_retries=3`, verifica que el error menciona "3 reintentos" o "agotados" |
| **CA4** | `invoke_with_retry_backoff_doubles_delay` | Con `retry_delay_base=1s`, mide `elapsed >= 1s` para confirmar que `tokio::time::sleep` se ejecuta |
| **CA5** | `save_agent_decision_creates_file_with_content` | Usa `TempDir`, crea decisión, verifica: archivo existe, contiene story ID, "✅ Éxito", stdout, actor |
| **CA5** | `save_agent_decision_noops_when_story_id_is_none` | Con `story_id: None`, verifica que no se crean archivos |
| **CA5** | `save_agent_decision_noops_when_decisions_dir_is_none` | Con `decisions_dir: None`, verifica que no paniquea |
| **CA6** | `tokio_is_a_dependency` | `#[test]` — importa `tokio::time::Duration` y `tokio::process::Command`. Si tokio no está en `Cargo.toml`, no compila |
| **CA7** | `invoke_with_retry_fails_when_agent_not_installed` | Adaptado a `#[tokio::test] async` con `.await` (antes era `#[test]` síncrono) |
| **CA8** | *No es un test unitario* | Es `cargo test --lib agent` — lo verificará el Developer |

### Total: 13 tests (3 conservados + 10 nuevos/adaptados)

### Lo que el Developer necesita para que compilen:
1. Añadir `tokio` a `Cargo.toml` con features `process`, `time`, `fs`, `rt`, `macros`
2. Hacer `async` las funciones `invoke_once`, `invoke_with_retry`, y `save_agent_decision`
3. Reemplazar `std::process::Command` → `tokio::process::Command`, `std::thread::sleep` → `tokio::time::sleep`, `std::fs::write` → `tokio::fs::write`
4. Reemplazar el loop `try_wait` + `thread::sleep` → `tokio::time::timeout`

```
