# Decisión de implementación — STORY-007 (Developer)

**Fecha**: 2026-05-04
**Rol**: Developer
**Transición**: Tests Ready → In Review

---

## Resumen

Implementación del trait `Workflow` y struct `CanonicalWorkflow` en `src/domain/workflow.rs`. Los tests fueron escritos por QA (20 tests) y la implementación los hace pasar replicando exactamente el comportamiento de `pipeline::next_status()` y `pipeline::map_status_to_role()`.

---

## ¿Qué se implementó?

### `Workflow` trait (`src/domain/workflow.rs`)

Trait público con 3 métodos (todos `&self`, inmutables — satisface CA7):

| Método | Firma | Propósito |
|--------|-------|-----------|
| `next_status` | `fn next_status(&self, current: Status) -> Status` | Infiere el estado tras la intervención del agente |
| `map_status_to_role` | `fn map_status_to_role(&self, status: Status) -> &'static str` | Mapea estado → nombre de rol canónico |
| `canonical_column_order` | `fn canonical_column_order(&self) -> &[&'static str]` | Orden visual de columnas para el board |

### `CanonicalWorkflow` struct

Implementación por defecto del trait. Sin estado interno (struct unit + `#[derive(Default)]`).

**`next_status()`** replica la función privada `pipeline::next_status()`:
- Draft → Ready
- Ready → TestsReady
- TestsReady → InReview
- InProgress → InReview
- InReview → BusinessReview
- BusinessReview → Done
- Terminales/Blocked → se quedan como están (`_ => current`)

**`map_status_to_role()`** replica la función privada `pipeline::map_status_to_role()`:
- Draft, BusinessReview → `"product_owner"`
- Ready → `"qa_engineer"`
- TestsReady, InProgress → `"developer"`
- InReview → `"reviewer"`
- Fallback → `"product_owner"`

**`canonical_column_order()`** devuelve un slice estático con las 9 columnas:
`["Draft", "Ready", "Tests Ready", "In Progress", "In Review", "Business Review", "Done", "Blocked", "Failed"]`

---

## Decisiones de diseño

1. **Struct unit sin estado**: `CanonicalWorkflow` no tiene campos. Es un struct unit (`;`) derivando `Default`. Esto es suficiente porque el comportamiento canónico solo depende del `Status` recibido como argumento.

2. **`#[allow(dead_code)]` en trait y struct**: clippy advierte que el trait y el struct no tienen consumidores externos en producción aún. Se añadió `#[allow(dead_code)]` como medida temporal. Cuando se migren `pipeline.rs` y `board.rs` a usar `&dyn Workflow`, estos atributos se podrán eliminar.

3. **No se modificó `pipeline.rs` ni `board.rs`**: la historia pide solo definir el trait y el struct, no migrar el código existente. La migración será una historia separada o parte de #04 (workflow configurable).

4. **Tests en el mismo archivo**: los tests de QA viven en `src/domain/workflow.rs` bajo `#[cfg(test)]`. Esto es consistente con el resto del proyecto (los tests de `state.rs`, `story.rs`, etc. siguen el mismo patrón).

---

## Verificación

| Comando | Resultado |
|---------|-----------|
| `cargo build` | ✅ Compila sin errores |
| `cargo test` | ✅ 208/208 pasan (197 unit + 11 architecture), 1 ignorado |
| `cargo clippy -- -D warnings` | ✅ Sin warnings |
| `cargo fmt` | ✅ Sin cambios pendientes |

---

## Notas para el Reviewer

- El `#[allow(dead_code)]` es intencional y temporal: desaparecerá cuando `pipeline.rs` use `&dyn Workflow`.
- Los tests provienen del QA. Se ejecutaron y pasan. No se modificó ningún test.
- La implementación es mínima: 3 match arms en `next_status`, 4 en `map_status_to_role`, un array literal en `canonical_column_order`.
