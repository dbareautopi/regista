# STORY-022 — developer — 20260505T125631

## Resultado
✅ Éxito


### Intento 1 (exit code: 0)

```

```

### stdout (intento 1)

```
Resumen de la **18ª verificación** de STORY-022:

---

### ✅ Código de producción: completo y correcto

| Verificación | Resultado |
|---|---|
| `cargo build` | ✅ OK |
| `cargo clippy --no-deps --bin regista` | ✅ 0 warnings |
| `cargo fmt -- --check` | ✅ OK |
| `cargo test --test architecture` | ✅ 11/11 |

La implementación cubre los 10 CAs implementables (CA1-CA8, CA10-CA11):
- `invoke_with_retry()` + `invoke_with_retry_blocking()` con `verbose: bool`
- `invoke_once()` con rama `verbose=false` (`wait_with_output`) y `verbose=true` (`invoke_once_verbose`)
- Streaming línea a línea con `BufReader::new()` + `read_line()` + `tracing::info!("  │ {}", trimmed)`
- stderr en `tokio::spawn` separado sin streaming
- `kill_process_by_pid()` cross-platform para timeout
- Call sites en `plan.rs` y `pipeline.rs` actualizados con `false`
- `io-util` en features de tokio

---

### ❌ Tests del QA: siguen sin compilar (18ª iteración)

**3 errores E0716** — `MutexGuard` temporal destruido antes que el `Cow<str>`:

| Test | Línea |
|------|-------|
| `ca3_verbose_logs_lines_with_pipe_prefix` | 1763 |
| `ca3_empty_lines_not_logged` | 1809 |
| `ca5_stderr_not_streamed_to_log` | 2006 |

**Solución requerida (QA):**
```rust
let binding = buffer.lock().unwrap();
let log_output = String::from_utf8_lossy(&binding);
```

---

**NO se avanza a In Review.** Documentado en:
- `STORY-022.md` (Activity Log)
- `.regista/decisions/STORY-022-dev-verification-18-2026-05-05.md`

El orquestador debe pasar el turno al QA para que corrija los 3 errores E0716.

```
