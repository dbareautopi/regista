# STORY-010 — Reviewer — 2026-05-05

## Revisión técnica: In Review → Business Review

### Verificaciones técnicas

| Verificación | Resultado |
|---|---|
| `cargo build` | ✅ Compila sin errores |
| `cargo test` (290 tests) | ✅ 290/290 pasan, 0 fallos, 1 ignorado |
| `cargo clippy -- -D warnings` | ✅ 0 warnings |
| `cargo fmt --check` | ✅ Formato correcto |

### Verificación de CAs

| CA | Descripción | Verificación |
|----|-------------|--------------|
| CA1 | `invoke_once()` async con `tokio::process::Command` | ✅ Línea 247-280 de `src/infra/agent.rs`. Función `async fn` que construye `tokio::process::Command::new()` y usa `.spawn()` + `child.wait_with_output()`. |
| CA2 | `tokio::time::timeout` en lugar de busy-polling | ✅ Línea 264. `tokio::time::timeout(timeout, child.wait_with_output()).await`. Zero `thread::sleep` ni `try_wait`. Timeout mata proceso vía `kill -9` (PID) por plataforma. |
| CA3 | `invoke_with_retry()` async con `tokio::time::sleep` | ✅ Línea 67-143. `pub async fn invoke_with_retry()`. Backoff con `tokio::time::sleep(delay).await` (línea 140). |
| CA4 | Backoff exponencial `delay *= 2` preservado | ✅ Línea 141. `delay *= 2` después de cada `tokio::time::sleep`. |
| CA5 | `save_agent_decision()` con `tokio::fs` | ✅ Línea 194-231. `async fn save_agent_decision()` usa `tokio::fs::create_dir_all()` y `tokio::fs::write()`. |
| CA6 | `tokio` en `Cargo.toml` | ✅ `Cargo.toml` línea 25: `tokio = { version = "1", features = ["rt-multi-thread", "macros", "process", "time", "fs"] }`. |
| CA7 | Tests adaptados a `#[tokio::test]` | ✅ 10 tests async (`#[tokio::test]`) + 3 tests sync preservados + 1 test `tokio_is_a_dependency`. |
| CA8 | `cargo test --lib agent` pasa | ✅ 13/14 tests pasan, 1 ignorado (`invoke_with_retry_fails_when_agent_not_installed` — requiere `pi`). |

### Wrapper síncrono

El módulo incluye `invoke_with_retry_blocking()` (línea 150-162) que usa un `LazyLock<Runtime>` global para que los callers síncronos (`plan.rs`, `pipeline.rs`) sigan funcionando sin cambios. Esto es un puente correcto hasta que esos módulos se migren a async.

### Conclusión

✅ **APROBADO → Business Review**

Todos los CAs satisfechos. Sin regresiones. Código limpio, compila, tests pasan, clippy y fmt sin issues. La migración a tokio elimina el busy-polling con `thread::sleep` sin romper la compatibilidad hacia atrás gracias al wrapper `invoke_with_retry_blocking()`.
