# STORY-008 — qa-engineer — 20260504T232415

## Resultado
✅ Éxito


### Intento 1 (exit code: 0)

```

```

### stdout (intento 1)

```
La estructura es correcta. Aquí está el resumen de lo implementado:

---

## Tests añadidos en `src/app/pipeline.rs` → `mod story008`

| # | Test | CAs cubiertos | Qué verifica |
|---|------|---------------|--------------|
| 1 | `apply_automatic_transitions_unblock_uses_workflow` | CA1, CA3 | Llama directamente a `apply_automatic_transitions` con `&dyn Workflow`, verifica que el target de desbloqueo viene de `workflow.next_status(Blocked)` |
| 2 | `apply_automatic_transitions_keeps_blocked_with_unresolved_deps` | CA3 | Edge case: historia Blocked con dependencia en Draft permanece Blocked |
| 3 | `apply_automatic_transitions_unblock_target_varies_by_workflow` | CA1, CA4 | Con `AltWorkflow` (Blocked→Draft) vs `CanonicalWorkflow` (Blocked→Ready) demuestra que el target NO está hardcodeado |
| 4 | `role_resolution_chain_uses_workflow_mapping` | CA2 | Verifica la cadena completa: status → `workflow.map_status_to_role()` → `provider_for_role()` → `skill_for_role()` para los 6 estados accionables |
| 5 | `role_resolution_changes_when_workflow_mapping_differs` | CA2 | `AltWorkflow` mapea `TestsReady` → `"reviewer"` (no `"developer"`) y verifica que el `skill_path` cambia |

## Cobertura final por CA

| CA | Tests (existentes + nuevos) |
|----|-----------------------------|
| CA1 | `canonical_workflow_provides_all_required_methods`, `canonical_workflow_usable_as_trait_object`, **`apply_automatic_transitions_unblock_uses_workflow`**, **`apply_automatic_transitions_unblock_target_varies_by_workflow`** |
| CA2 | `workflow_role_mapping_covers_all_states_process_story_handles`, `workflow_role_mapping_is_deterministic`, **`role_resolution_chain_uses_workflow_mapping`**, **`role_resolution_changes_when_workflow_mapping_differs`** |
| CA3 | `unblock_target_comes_from_workflow_not_hardcoded`, `workflow_next_status_handles_terminal_states`, **`apply_automatic_transitions_unblock_uses_workflow`**, **`apply_automatic_transitions_keeps_blocked_with_unresolved_deps`** |
| CA4 | `unblock_target_comes_from_workflow_not_hardcoded`, `unblock_target_changes_when_workflow_changes`, **`apply_automatic_transitions_unblock_target_varies_by_workflow`** |
| CA5 | `next_status_follows_happy_path`, `next_status_fix_path`, `canonical_workflow_matches_original_hardcoded_behavior` |
| CA6 | Ejecución (`cargo test --lib pipeline`) |
| CA7 | Ejecución (`cargo build`) |

**Notas para el Developer:**
- Los nuevos tests llaman a `apply_automatic_transitions` con un 6º parámetro `&dyn Workflow` que aún no existe en la firma → no compilarán hasta que añadas el parámetro.
- `CanonicalWorkflow::next_status(Blocked)` actualmente retorna `Blocked` (vía `_ => current`). Los tests (tanto existentes como nuevos) esperan `Status::Ready`. Debes actualizar `CanonicalWorkflow` en `domain/workflow.rs`.
- Los `AltWorkflow` definidos inline en los tests son mínimos (solo para testing, sin lógica de negocio).

```
