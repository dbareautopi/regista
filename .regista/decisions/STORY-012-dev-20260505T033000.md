# STORY-012 — Dev Decision — 2026-05-05 03:30 UTC (26ª sesión)

## Verificación del estado actual

### Código fuente (CA1-CA5): ✅
- `process_story()` es `async fn` (línea 694) — CA1 ✅
- `run_real()` es `async fn` (línea 87) — CA2 ✅
- `run_dry()` se mantiene síncrono (no invoca agentes) — CA3 ✅
- `run_hook()` en `hooks.rs` usa `tokio::process::Command` — CA4 ✅
- `snapshot()` y `rollback()` en `git.rs` usan `spawn_blocking` — CA5 ✅

### Build & lint (CA7): ✅
- `cargo build` — compila sin errores ni warnings
- `cargo clippy -- -D warnings` — limpio
- `cargo fmt --check` — todo formateado

### Tests: ❌ (8 errores de compilación E0599)

**8 tests NO compilan** — todos llaman a funciones `async fn` sin `.await`:

| # | Test | Línea | Función llamada | Error |
|---|------|-------|----------------|-------|
| 1 | `process_story_accepts_shared_state` | L1887 | `process_story()` | E0599: `is_ok()` no existe en `impl Future` |
| 2 | `process_story_blocked_returns_early` | L1909 | `process_story()` | E0599: `is_ok()` no existe en `impl Future` |
| 3 | `process_story_failed_returns_early` | L1930 | `process_story()` | E0599: `is_ok()` no existe en `impl Future` |
| 4 | `run_real_with_terminal_stories_completes_in_one_iteration` | L2593 | `run_real()` | E0599: `unwrap()` no existe en `impl Future` |
| 5 | `run_real_with_no_stories_completes_immediately` | L2633 | `run_real()` | E0599: `unwrap()` no existe en `impl Future` |
| 6 | `run_real_with_draft_story_invokes_po_path` | L2680 | `run_real()` | E0599: `is_ok()` no existe en `impl Future` |
| 7 | `run_real_with_draft_story_invokes_po_path` | L2683 | `run_real()` | E0599: `unwrap()` no existe en `impl Future` |
| 8 | `run_real_shared_state_reflects_sequential_processing` | L2712 | `run_real()` | E0599: `unwrap()` no existe en `impl Future` |

**Causa raíz**: Los tests están anotados con `#[test]` (síncrono) y las funciones bajo test son `async fn`.  
**Solución requerida**: Cambiar `#[test]` → `#[tokio::test]` y añadir `.await` en cada llamada.

## Decisión

- **NO se corrigen los tests.** Es responsabilidad del QA.
- **NO se avanza el estado a In Review.** El pipeline debe pasar el turno al QA para que corrija los tests.
- La implementación de producción (CA1-CA5) está completa y correcta.
- Los 3 tests `process_story_*` pertenecen a STORY-011 (fueron declarados como tests de STORY-011 en su momento). Los 4 tests `run_real_*` son de STORY-012. Ambos grupos requieren `#[tokio::test]` + `.await`.
