# STORY-010 — developer — 20260505T013818

## Resultado
✅ Éxito


### Intento 1 (exit code: 0)

```

```

### stdout (intento 1)

```
---

## Resumen de STORY-010 — Implementación completada (status: **Tests Ready**, sin avanzar)

### Qué se implementó

Migración completa de `src/infra/agent.rs` de busy-polling síncrono a `tokio` async:

| Archivo | Cambio |
|---------|--------|
| **`Cargo.toml`** | Añadido `tokio` v1 con features: `rt-multi-thread`, `macros`, `process`, `time`, `fs` |
| **`src/infra/agent.rs`** | `invoke_once()` → `async fn` con `tokio::process::Command` + `tokio::time::timeout` |
| | `invoke_with_retry()` → `async fn` con `tokio::time::sleep` (backoff exponencial) |
| | `save_agent_decision()` → `async fn` con `tokio::fs::write` |
| | Añadido `invoke_with_retry_blocking()` wrapper síncrono con `LazyLock<Runtime>` |
| | Eliminados `std::thread::sleep` y busy-polling `try_wait` |
| **`src/app/plan.rs`** | `invoke_with_retry` → `invoke_with_retry_blocking` |
| **`src/app/pipeline.rs`** | `invoke_with_retry` → `invoke_with_retry_blocking` |

### Resultados de tests

- ✅ **278/280 tests pasan** (0 regresiones)
- ⚠️ **1 test falla por bug del QA** (no de implementación): `save_agent_decision_creates_file_with_content`
- ⏭️ 1 ignorado (`invoke_with_retry_fails_when_agent_not_installed` — requiere `pi`)

### Por qué no se avanza a InReview

El test `save_agent_decision_creates_file_with_content` tiene un error de diseño: usa `decisions_dir = tmp.path()` (raíz del tempdir) y también crea `product-owner/SKILL.md` ahí mismo. `read_dir()` cuenta 2 entradas en vez de 1. El **fix** es trivial para el QA: usar `tmp.path().join("decisions")` o filtrar con `.is_file()`.

Documentado en `.regista/decisions/STORY-010-dev-20260505T000000.md`. El orquestador debe pasar el turno al QA automáticamente.

```
