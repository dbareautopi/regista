# STORY-012 Dev Decision — 18ª sesión

**Fecha**: 2026-05-05
**Rol**: Developer

## Estado de la implementación

La implementación async (CA1-CA5) está completada desde sesiones anteriores y no ha cambiado:

- **CA1** ✅: `process_story()` es `async fn` que invoca `invoke_with_retry(...).await`
- **CA2** ✅: Loop principal `run_real()` usa `process_story(...).await` secuencialmente
- **CA3** ✅: `run_dry()` se mantiene síncrono (no invoca agentes reales)
- **CA4** ✅: `run_hook()` se ejecuta con `tokio::task::spawn_blocking` (evita panic por nested runtime)
- **CA5** ✅: `snapshot()` y `rollback()` en `git.rs` se ejecutan con `spawn_blocking`

## Verificaciones

| Verificación | Resultado |
|---|---|
| `cargo build` | ✅ Compila sin errores ni warnings |
| `cargo clippy -- -D warnings` | ✅ Pasa limpio |
| `cargo fmt --check` | ✅ Pasa limpio |
| `cargo test` | ❌ 8 tests no compilan (E0599) |

## Tests que fallan (todos E0599 — async fn sin .await)

### STORY-011 (3 tests)
| # | Test | Línea | Problema |
|---|---|---|---|
| 1 | `process_story_accepts_shared_state` | L1887 | `process_story(...).is_ok()` → falta `.await` + `#[tokio::test]` |
| 2 | `process_story_blocked_returns_early` | L1909 | `process_story(...).is_ok()` → falta `.await` + `#[tokio::test]` |
| 3 | `process_story_failed_returns_early` | L1930 | `process_story(...).is_ok()` → falta `.await` + `#[tokio::test]` |

### STORY-012 (5 tests)
| # | Test | Línea | Problema |
|---|---|---|---|
| 4 | `run_real_with_terminal_stories_completes_in_one_iteration` | L2593 | `run_real(...).unwrap()` → falta `.await` + `#[tokio::test]` |
| 5 | `run_real_with_no_stories_completes_immediately` | L2633 | `run_real(...).unwrap()` → falta `.await` + `#[tokio::test]` |
| 6 | `run_real_with_draft_story_invokes_po_path` | L2680 | `result.is_ok()` → falta `.await` + `#[tokio::test]` |
| 7 | `run_real_with_draft_story_invokes_po_path` | L2683 | `result.unwrap()` → falta `.await` + `#[tokio::test]` |
| 8 | `run_real_shared_state_reflects_sequential_processing` | L2712 | `run_real(...).unwrap()` → falta `.await` + `#[tokio::test]` |

## Corrección necesaria (trabajo del QA)

Los 8 tests requieren dos cambios:

1. Cambiar `#[test]` por `#[tokio::test]`
2. Añadir `.await` en las llamadas a funciones async:
   - `process_story(...)` → `process_story(...).await`
   - `run_real(...)` → `run_real(...).await`

## Nota adicional

El test `process_story_does_not_block_runtime` ya compila correctamente (se corrigió en la 11ª sesión añadiendo `Send + Sync` a los traits `Workflow` y `AgentProvider`, y envolviendo `run_hook()` en `spawn_blocking`).

## Estado final

NO se avanza a In Review. El orquestador debe pasar el turno al QA automáticamente para que corrija los tests.
