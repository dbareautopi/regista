# STORY-012 — Decisión del Developer (15ª sesión)

**Timestamp**: 2026-05-05T03:06:58Z

## Resultado

NO se avanza a In Review. Los tests no compilan.

## Estado de la implementación

La implementación async de `process_story()` y `run_real()` ya está completada (sesiones 1–13).

- `cargo build` — limpio ✅
- `cargo clippy -- -D warnings` — limpio ✅  
- `cargo fmt --check` — limpio ✅

CA1–CA5 implementados sin cambios desde sesiones anteriores.

## Tests que NO compilan (8 errores E0599)

Todos los errores son del mismo tipo: tests con `#[test]` (síncronos) que llaman a funciones `async fn` sin `.await`. La solución es añadir `#[tokio::test]` y `.await` en cada call site. Es trabajo del QA.

### story011 (3 tests)

| # | Línea | Test | Call sin .await |
|---|-------|------|-----------------|
| 1 | L1887 | `process_story_accepts_shared_state` | `process_story(...).is_ok()` |
| 2 | L1909 | `process_story_blocked_returns_early` | `process_story(...).is_ok()` |
| 3 | L1930 | `process_story_failed_returns_early` | `process_story(...).is_ok()` |

### story012 (5 tests)

| # | Línea | Test | Call sin .await |
|---|-------|------|-----------------|
| 4 | L2593 | `run_real_with_terminal_stories_completes_in_one_iteration` | `run_real(...).unwrap()` |
| 5 | L2633 | `run_real_with_no_stories_completes_immediately` | `run_real(...).unwrap()` |
| 6 | L2680 | `run_real_with_draft_story_invokes_po_path` | `result.is_ok()` |
| 7 | L2683 | `run_real_with_draft_story_invokes_po_path` | `result.unwrap()` |
| 8 | L2712 | `run_real_shared_state_reflects_sequential_processing` | `run_real(...).unwrap()` |

## Corrección necesaria (trabajo del QA)

Cada test necesita:
1. Cambiar `#[test]` → `#[tokio::test]`
2. Cambiar `fn test_name() {` → `async fn test_name() {`
3. Añadir `.await` tras cada llamada a `process_story(...)` o `run_real(...)`

Ejemplo para el test de L1887:
```rust
// Antes:
#[test]
fn process_story_accepts_shared_state() {
    let result = process_story(&story, tmp.path(), &cfg, &state, &agent_opts, &workflow);
    assert!(result.is_ok(), ...);
}

// Después:
#[tokio::test]
async fn process_story_accepts_shared_state() {
    let result = process_story(&story, tmp.path(), &cfg, &state, &agent_opts, &workflow).await;
    assert!(result.is_ok(), ...);
}
```

## Estado

- Status actual: **Tests Ready**
- NO se avanza a In Review
- El orquestador debe pasar el turno al QA para corregir los tests
