# STORY-012 — Dev Decision — 28ª sesión

**Fecha**: 2026-05-05T03:35:00Z
**Rol**: Developer
**Transición**: Tests Ready → (sin avance, bloqueado por tests)

---

## Verificación del código fuente

| Verificación | Resultado |
|---|---|
| `cargo build` | ✅ Compila sin errores ni warnings |
| `cargo clippy -- -D warnings` | ✅ Sin warnings |
| `cargo fmt --check` | ✅ Formato correcto |
| `cargo run -- run --dry-run` | ✅ 18 historias → Done, 59 iteraciones |

La implementación async de CA1-CA5 ya está completada y funciona correctamente:
- `process_story()` es `async fn` que usa `invoke_with_retry(...).await`
- `run_real()` es `async fn` con loop secuencial (`.await`, sin `tokio::spawn`)
- `run_dry()` se mantiene síncrono (no invoca agentes reales)
- `run_hook()` usa `spawn_blocking` wrapping (STORY-011 ya lo migró)
- `git.rs` (snapshot/rollback) usa `spawn_blocking`

---

## Tests que NO compilan

8 errores E0599 — llamadas a funciones `async fn` sin `.await` en tests `#[test]` (no `#[tokio::test]`):

### STORY-011 (3 errores en 3 tests)

| Línea | Test | Función llamada | Corrección necesaria |
|---|---|---|---|
| ~1887 | `process_story_accepts_shared_state` | `process_story(...)` → `.is_ok()` | `#[tokio::test]` + `.await` |
| ~1909 | `process_story_blocked_returns_early` | `process_story(...)` → `.is_ok()` | `#[tokio::test]` + `.await` |
| ~1930 | `process_story_failed_returns_early` | `process_story(...)` → `.is_ok()` | `#[tokio::test]` + `.await` |

### STORY-012 (5 errores en 4 tests)

| Línea | Test | Función llamada | Corrección necesaria |
|---|---|---|---|
| ~2593 | `run_real_with_terminal_stories_completes_in_one_iteration` | `run_real(...).unwrap()` | `#[tokio::test]` + `.await` |
| ~2633 | `run_real_with_no_stories_completes_immediately` | `run_real(...).unwrap()` | `#[tokio::test]` + `.await` |
| ~2680, 2683 | `run_real_with_draft_story_invokes_po_path` | `run_real(..).is_ok()` / `.unwrap()` | `#[tokio::test]` + `.await` (2 errores) |
| ~2712 | `run_real_shared_state_reflects_sequential_processing` | `run_real(...).unwrap()` | `#[tokio::test]` + `.await` |

**Causa raíz**: todas estas funciones (`process_story`, `run_real`) fueron migradas a `async fn` (CA1-CA2). Los tests que las invocan deben usar `#[tokio::test]` y añadir `.await` a las llamadas. El compilador incluso sugiere la corrección exacta en cada error.

**NO se corrigen los tests** — es trabajo del QA. El orquestador debe pasar el turno al QA para que:
1. Cambie `#[test]` → `#[tokio::test]` en los 7 tests afectados
2. Añada `.await` en las llamadas a `process_story()` y `run_real()`

---

## Conclusión

- Código fuente: ✅ correcto, compila, lint limpio, dry-run correcto
- Tests: ❌ 7 tests necesitan actualización de firma (`#[tokio::test]` + `.await`)
- NO se avanza a In Review
