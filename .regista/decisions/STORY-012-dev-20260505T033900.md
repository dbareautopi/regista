# STORY-012 — Decisión Dev (30ª sesión)

**Fecha:** 2026-05-05T03:39:00Z
**Rol:** Developer
**Transición:** Tests Ready → (no se avanza)

## Estado actual del código fuente

| Verificación | Resultado |
|---|---|
| `cargo build` | ✅ Compila limpio |
| `cargo clippy -- -D warnings` | ✅ Sin warnings |
| `cargo fmt --check` | ✅ Formato correcto |
| `cargo run -- run --dry-run` | ✅ 18 historias → Done, 59 iteraciones |

La implementación async de `process_story()` y `run_real()` (CA1-CA5) está completa y correcta desde sesiones previas.

## Tests que no compilan (8 errores E0599)

Todos los errores son **exclusivamente en tests**, ninguno en el código fuente. Los tests llaman a funciones `async fn` sin `.await` ni `#[tokio::test]`.

### Tests de STORY-011 (3 errores en `pipeline.rs`)

| Test | Línea aprox. | Función llamada | Problema |
|---|---|---|---|
| `process_story_accepts_shared_state` | ~L1887 | `process_story()` (async) | Falta `.await` |
| `process_story_blocked_returns_early` | ~L1909 | `process_story()` (async) | Falta `.await` |
| `process_story_failed_returns_early` | ~L1930 | `process_story()` (async) | Falta `.await` |

### Tests de STORY-012 (5 errores en 4 tests, `pipeline.rs`)

| Test | Línea aprox. | Función llamada | Problema |
|---|---|---|---|
| `run_real_with_terminal_stories_completes_in_one_iteration` | ~L2593 | `run_real()` (async) | Falta `.await` |
| `run_real_with_no_stories_completes_immediately` | ~L2633 | `run_real()` (async) | Falta `.await` |
| `run_real_with_draft_story_invokes_po_path` | ~L2680, ~L2683 | `run_real()` (async) | 2 errores, falta `.await` |
| `run_real_shared_state_reflects_sequential_processing` | ~L2712 | `run_real()` (async) | Falta `.await` |

## Corrección necesaria (responsabilidad del QA)

Los tests necesitan:
1. Cambiar `#[test]` por `#[tokio::test]`
2. Añadir `.await` a las llamadas a `process_story(...)` y `run_real(...)`

Ejemplo:
```rust
// Antes:
#[test]
fn process_story_accepts_shared_state() {
    let result = process_story(&story, tmp.path(), &cfg, &state, &agent_opts, &workflow);
    assert!(result.is_ok());
}

// Después:
#[tokio::test]
async fn process_story_accepts_shared_state() {
    let result = process_story(&story, tmp.path(), &cfg, &state, &agent_opts, &workflow).await;
    assert!(result.is_ok());
}
```

## Decisión

**NO se avanza a In Review.** Los tests no compilan. Es trabajo del QA corregirlos. El orquestador se encargará de pasar el turno al QA automáticamente (transición Tests Ready → Tests Ready con feedback).
