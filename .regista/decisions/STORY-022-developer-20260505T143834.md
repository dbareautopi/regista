# STORY-022 — developer — 20260505T143834

## Resultado
✅ Éxito


### Intento 1 (exit code: 0)

```

```

### stdout (intento 1)

```
---

## Resumen — STORY-022

**Estado**: **Tests Ready** (sin cambios, no se avanza a In Review)

**Verificación del código de producción**:

| Comando | Resultado |
|---------|-----------|
| `cargo check` | ✅ OK, 0 errores |
| `cargo build` | ✅ OK, binario generado |
| `cargo clippy --no-deps --bin regista` | ✅ OK, 0 warnings |
| `cargo fmt -- --check` | ✅ OK, formateado |
| `cargo test --test architecture` | ✅ 11/11 pasan |

**Implementación**: El código de producción ya cubre CA1-CA8 y CA10-CA11. Las funciones `invoke_with_retry()`, `invoke_with_retry_blocking()`, e `invoke_once()` aceptan `verbose: bool`. `invoke_once_verbose()` hace streaming línea a línea de stdout con `BufReader::new()` + `read_line()` y emite cada línea no vacía con `tracing::info!("  │ {}", trimmed)`. stderr se lee en `tokio::spawn` separado. Los call sites en `plan.rs` y `pipeline.rs` pasan `false`. `kill_process_by_pid()` maneja timeout cross-platform.

**Tests del QA — 3 errores E0716 que bloquean `cargo test`**:

| # | Test | Línea | Problema |
|---|------|-------|----------|
| 1 | `ca3_verbose_logs_lines_with_pipe_prefix` | 1763 | `MutexGuard` temporal destruido |
| 2 | `ca3_empty_lines_not_logged` | 1809 | `MutexGuard` temporal destruido |
| 3 | `ca5_stderr_not_streamed_to_log` | 2006 | `MutexGuard` temporal destruido |

Las 3 líneas tienen el mismo patrón: `String::from_utf8_lossy(&buffer.lock().unwrap())` — el `MutexGuard` se destruye al final del statement mientras el `Cow<str>` aún lo referencia. Solución requerida (QA):

```rust
let binding = buffer.lock().unwrap();
let log_output = String::from_utf8_lossy(&binding);
```

**NO se avanza a In Review**. Documentado en:
- `STORY-022.md` — Activity Log (24ª verificación)
- `.regista/decisions/STORY-022-dev-verification-24-2026-05-05.md`

```
