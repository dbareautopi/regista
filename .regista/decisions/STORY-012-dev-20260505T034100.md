# STORY-012 — Decisión Dev (31ª sesión)

**Fecha:** 2026-05-05T03:41:00Z
**Rol:** Developer
**Transición:** Tests Ready → (no se avanza)

## Estado actual del código fuente

| Verificación | Resultado |
|---|---|
| `cargo build` | ✅ Compila limpio |
| `cargo clippy -- -D warnings` | ✅ Sin warnings |
| `cargo fmt --check` | ✅ Formato correcto |
| `cargo run -- run --dry-run` | ✅ 18 historias → Done, 59 iteraciones |

La implementación async de `process_story()` y `run_real()` (CA1-CA5) está completa y correcta desde sesiones previas. No se requirieron cambios en el código fuente.

## Tests que no compilan (8 errores E0599)

Todos los errores son **exclusivamente en tests**, ninguno en el código fuente. Los tests llaman a funciones `async fn` sin `.await` ni `#[tokio::test]`.

### Tests de STORY-011 (3 errores en `src/app/pipeline.rs`)

| Test | Línea test | Línea error | Función llamada | Problema |
|---|---|---|---|---|
| `process_story_accepts_shared_state` | L1871 | L1887 | `process_story()` (async) | `.is_ok()` sobre `Future` — falta `.await` |
| `process_story_blocked_returns_early` | L1894 | L1909 | `process_story()` (async) | `.is_ok()` sobre `Future` — falta `.await` |
| `process_story_failed_returns_early` | L1915 | L1930 | `process_story()` (async) | `.is_ok()` sobre `Future` — falta `.await` |

### Tests de STORY-012 (5 errores en 4 tests, `src/app/pipeline.rs`)

| Test | Línea test | Línea error | Función llamada | Problema |
|---|---|---|---|---|
| `run_real_with_terminal_stories_completes_in_one_iteration` | L2570 | L2593 | `run_real()` (async) | `.unwrap()` sobre `Future` — falta `.await` |
| `run_real_with_no_stories_completes_immediately` | L2624 | L2633 | `run_real()` (async) | `.unwrap()` sobre `Future` — falta `.await` |
| `run_real_with_draft_story_invokes_po_path` | L2647 | L2680 | `run_real()` (async) | `.is_ok()` sobre `Future` — falta `.await` |
| `run_real_with_draft_story_invokes_po_path` | L2647 | L2683 | `run_real()` (async) | `.unwrap()` sobre `Future` — falta `.await` |
| `run_real_shared_state_reflects_sequential_processing` | L2692 | L2712 | `run_real()` (async) | `.unwrap()` sobre `Future` — falta `.await` |

## Corrección necesaria (responsabilidad del QA)

Los tests necesitan:
1. Cambiar `#[test]` por `#[tokio::test]`
2. Añadir `async` a la firma de la función
3. Añadir `.await` a las llamadas a `process_story(...)` y `run_real(...)`

Ejemplo:
```rust
// Antes:
#[test]
fn run_real_with_terminal_stories_completes_in_one_iteration() {
    let report = run_real(tmp.path(), &cfg, &options, None).unwrap();
    assert_eq!(report.total, 3);
}

// Después:
#[tokio::test]
async fn run_real_with_terminal_stories_completes_in_one_iteration() {
    let report = run_real(tmp.path(), &cfg, &options, None).await.unwrap();
    assert_eq!(report.total, 3);
}
```

## Decisión

**NO se avanza a In Review.** Los tests no compilan. Es trabajo del QA corregirlos. El orquestador se encargará de pasar el turno al QA automáticamente (transición Tests Ready → Tests Ready con feedback).
