# STORY-012 — qa — 20260505T000000

## Resultado
✅ Éxito

## Resumen
Verificación de cobertura de tests para STORY-012 (migración de `pipeline.rs` a async).  
No se escribieron nuevos tests porque los 8 CAs ya están cubiertos por tests existentes:

| CA | Archivo | Tests | Estado |
|----|---------|-------|--------|
| CA1 | `src/app/pipeline.rs` (mod story012) | `process_story_is_async_and_returns_future`, `process_story_awaits_agent_and_propagates_result`, `process_story_does_not_block_runtime` | ✅ |
| CA2 | `src/app/pipeline.rs` (mod story012) | `run_real_processes_stories_one_at_a_time`, `run_real_continues_after_individual_story_error`, `run_real_with_terminal_stories_completes_in_one_iteration`, `run_real_with_no_stories_completes_immediately`, `run_real_with_draft_story_invokes_po_path`, `run_real_shared_state_reflects_sequential_processing` | ✅ |
| CA3 | `src/app/pipeline.rs` (mod story012) | `run_dry_remains_callable_without_tokio_runtime`, `run_dry_with_stories_produces_valid_report` | ✅ |
| CA4 | `src/infra/hooks.rs` | 6 tests (`run_hook_with_none_returns_ok`, `run_hook_executes_valid_command`, `run_hook_fails_with_invalid_command`, `run_hook_executes_shell_commands`, `run_hook_callable_from_sync_context`, `run_hook_safe_from_async_context`) | ✅ |
| CA5 | `src/infra/git.rs` | 7 tests (`snapshot_creates_commit_when_changes_exist`, `snapshot_without_changes_returns_current_hash`, `rollback_restores_previous_state`, `rollback_with_invalid_hash_returns_false`, `snapshot_without_git_returns_none`, `snapshot_and_rollback_safe_from_async_context`, `concurrent_snapshots_dont_deadlock`) | ✅ |
| CA6 | — | Verificación por ejecución (`cargo test --lib app`) | 📋 |
| CA7 | — | Verificación por ejecución (`cargo build`, `cargo clippy`) | 📋 |
| CA8 | `src/app/pipeline.rs` (mod story012) | `run_report_structure_preserved_for_ci_compatibility`, `run_report_with_stop_reason_serializes_reason`, `run_report_without_stop_reason_omits_field` | ✅ |

## Notas para el Developer
Los tests en `mod story012` de `pipeline.rs` asumen que `process_story` y `run_real` ya son `async fn`.  
Actualmente son síncronos, por lo que esos tests NO compilarán hasta que se complete la migración.  
Eso es esperado: los tests definen el contrato, el Developer implementa para que compilen y pasen.

### Cambios requeridos por el Developer:
1. `process_story()` → `async fn`, usar `invoke_with_retry(...).await` en vez de `invoke_with_retry_blocking`
2. `run_real()` → `async fn`, usar `process_story(...).await` secuencialmente en el loop
3. `hooks.rs::run_hook()` → migrar internamente a `tokio::process::Command`
4. `git.rs::snapshot()` / `git.rs::rollback()` → migrar internamente a `spawn_blocking`
