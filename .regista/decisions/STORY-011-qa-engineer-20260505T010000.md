# STORY-011 — qa-engineer — 20260505T010000

## Resultado
✅ Tests existentes verificados — suficientes. No se requieren tests adicionales.

---

## Mapeo de CAs a tests existentes

### CA1: `SharedState` con `Arc<RwLock<HashMap<...>>>`

**Definición**: `src/domain/state.rs:79-89` (el struct ya existe con `#[derive(Debug, Clone, Default)]`)

**Tests**: `src/domain/state.rs → mod tests::mod story011` (3 tests)

| Test | Qué verifica |
|------|-------------|
| `shared_state_has_required_fields` | Los 3 campos existen con los tipos correctos: `Arc<RwLock<HashMap<String, u32>>>` (reject_cycles, story_iterations) y `Arc<RwLock<HashMap<String, String>>>` (story_errors). Verificación estática con type ascription. |
| `shared_state_default_is_empty` | `SharedState::default()` produce los 3 HashMaps vacíos, verificados con `read().unwrap().is_empty()`. |
| `shared_state_clone_shares_data` | Clone comparte el mismo Arc (escritura en clon → visible en original). Verifica el comportamiento `Arc` para uso concurrente futuro. |

✅ **Suficientes.** Cubren existencia tipada, defaults, y semántica de compartición.

---

### CA2: `process_story()` recibe `&SharedState` en lugar de `&mut HashMap<...>`

**Tests**: `src/app/pipeline.rs → mod tests::mod story011` (3 tests)

| Test | Qué verifica |
|------|-------------|
| `process_story_accepts_shared_state` | `Status::Done` → `Ok(())` sin invocar agente. Verifica que la firma `&SharedState` compila y el early-return funciona. |
| `process_story_blocked_returns_early` | `Status::Blocked` → `Ok(())` sin invocar agente. |
| `process_story_failed_returns_early` | `Status::Failed` → `Ok(())` sin invocar agente. |

✅ **Suficientes.** Cubren las 3 rutas de early-return. Las rutas con invocación real de agente (Draft→Ready, Ready→TestsReady, etc.) requieren un agente real o mock — fuera del scope de tests unitarios.

---

### CA3: `.read().unwrap()` / `.write().unwrap()` con locks de corta duración

**Tests en `src/domain/state.rs`** (5 tests) + **`src/app/pipeline.rs`** (1 test) = 6 tests

| Test (state.rs) | Qué verifica |
|-----------------|-------------|
| `read_lock_allows_reading` | `.read().unwrap()` devuelve guarda legible; valor insertado con write es visible. |
| `write_lock_allows_mutation` | `.write().unwrap()` permite insertar/modificar; cambios persisten tras soltar lock. |
| `lock_is_released_after_scope` | Lock se libera al salir del scope: write→read→write sin deadlock. 3 ciclos consecutivos. |
| `multiple_readers_allowed` | Dos `read()` locks simultáneos no deadlockean (RwLock lo permite). Ambos ven los mismos datos. |
| `write_lock_is_exclusive` | `try_read()` falla mientras hay un `write()` activo; tras drop, `try_read()` funciona. |

| Test (pipeline.rs) | Qué verifica |
|-------------------|-------------|
| `locks_are_released_after_apply_automatic_transitions` | Tras ejecutar `apply_automatic_transitions`, los locks de SharedState están libres para `read()` y `write()` de nuevo. |

✅ **Suficientes.** Cubren lectura, escritura, liberación de scope, multi-reader, exclusividad de writer, y liberación post-función.

---

### CA4: `apply_automatic_transitions()` accede a `reject_cycles` vía `SharedState`

**Tests**: `src/app/pipeline.rs → mod tests::mod story011` (4 tests)

| Test | Qué verifica |
|------|-------------|
| `apply_automatic_transitions_reads_reject_cycles_from_shared_state` | 8 ciclos (max_reject_cycles) → `Failed`. Verifica que la función lee desde `SharedState.reject_cycles`. |
| `apply_automatic_transitions_does_not_fail_below_threshold` | 5 < 8 → NO marca `Failed`, permanece `InReview`. |
| `apply_automatic_transitions_handles_missing_reject_cycles_entry` | Sin entrada en el mapa → 0 ciclos implícitos → no falla. |
| `apply_automatic_transitions_only_fails_stories_at_threshold` | Lote mixto: historia con 8 ciclos → `Failed`; historia con 7 → intacta. |

✅ **Suficientes.** Cubren umbral exacto, por debajo del umbral, entrada faltante, y lote mixto.

---

### CA5: `save_checkpoint()` clona contenido bajo `read()` lock para serializar

**Tests en `src/app/pipeline.rs`** (3 tests) + **`src/infra/checkpoint.rs`** (1 test) = 4 tests

| Test (pipeline.rs) | Qué verifica |
|-------------------|-------------|
| `save_checkpoint_clones_shared_state_under_read_lock` | Roundtrip: datos en SharedState → `save_checkpoint(&SharedState)` → `OrchestratorState::load()` recupera los 3 mapas correctamente. |
| `save_checkpoint_with_empty_state` | SharedState vacío → checkpoint con iteration=1, mapas vacíos. |
| `save_checkpoint_works_with_external_read_lock` | Con un `read()` lock externo activo en `story_iterations`, `save_checkpoint` no deadlockea (RwLock permite múltiples readers). |

| Test (checkpoint.rs) | Qué verifica |
|---------------------|-------------|
| `build_from_shared_state_locks` | Clon bajo `.read().unwrap()` → `OrchestratorState` construido correctamente → los 3 locks liberados (`try_read().is_ok()`). |

✅ **Suficientes.** Cubren roundtrip completo, estado vacío, concurrencia de readers, y liberación de locks.

---

### CA6: `cargo test --lib` pasa (tests de pipeline adaptados)

No automatizable como test unitario. Verificación manual del Developer:
- `cargo test --lib` — todos los tests pasan
- Nota: la referencia legacy `orchestrator` debe adaptarse a `app::pipeline`

---

### CA7: `cargo build` compila sin warnings

No automatizable como test unitario. Verificación manual del Developer:
- `cargo build`
- `cargo clippy -- -D warnings`
- `cargo fmt --check`

---

## Resumen

| CA | ¿Cubierto? | Ubicación | Nº tests |
|----|-----------|-----------|---------|
| CA1 | ✅ | `domain/state.rs::mod story011` | 3 |
| CA2 | ✅ | `app/pipeline.rs::mod story011` | 3 |
| CA3 | ✅ | `state.rs` (5) + `pipeline.rs` (1) | 6 |
| CA4 | ✅ | `app/pipeline.rs::mod story011` | 4 |
| CA5 | ✅ | `pipeline.rs` (3) + `checkpoint.rs` (1) | 4 |
| CA6 | N/A | Developer manual | — |
| CA7 | N/A | Developer manual | — |

**Total: 20 tests** en 3 módulos existentes. Sin módulos nuevos. Sin fake providers. Sin lógica de negocio implementada (solo el esqueleto de `SharedState` que ya existe en `domain/state.rs`).

## Estado actual de compilación

Los tests actualmente **no compilan** porque las funciones `process_story()`, `apply_automatic_transitions()` y `save_checkpoint()` todavía usan las firmas antiguas con `&mut HashMap<...>` / `&HashMap<...>`. Esto es esperado — el Developer debe actualizar esas firmas a `&SharedState` para que los tests compilen y pasen.

### Trabajo pendiente para el Developer

1. Cambiar firma de `process_story`: `reject_cycles: &mut HashMap<String, u32>` → `state: &SharedState`
2. Cambiar firma de `apply_automatic_transitions`: `reject_cycles: &mut HashMap<String, u32>` → `state: &SharedState`
3. Cambiar firma de `save_checkpoint`: 3 `&HashMap<...>` → `state: &SharedState`
4. Adaptar `run_real()`: construir `SharedState` desde `OrchestratorState` o usar `SharedState::default()`
5. Adaptar cuerpos: usar `.read().unwrap()` / `.write().unwrap()` en reemplazo de accesos directos a HashMap
6. Actualizar callers en `run_real` (2 sitios de `save_checkpoint`, 2 de `process_story`, 1 de `apply_automatic_transitions`)

### No se requieren tests adicionales.
