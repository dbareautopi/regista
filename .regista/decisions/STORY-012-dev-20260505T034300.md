# STORY-012 — Decisión de Implementación (32ª sesión)

**Fecha**: 2026-05-05T03:43:00Z
**Rol**: Developer
**Transición**: Tests Ready → (sin transición, tests no compilan)

---

## Estado de la implementación

La implementación async (CA1-CA5) ya está completada desde sesiones anteriores. Sin cambios en código fuente en esta sesión.

### Verificaciones realizadas

| Verificación | Resultado |
|---|---|
| `cargo build` | ✅ Compila sin errores |
| `cargo clippy -- -D warnings` | ✅ Sin warnings |
| `cargo fmt --check` | ✅ Sin cambios |
| `cargo run -- run --dry-run` | ✅ 18 historias → Done en 59 iteraciones (CA8) |

### Tests que fallan

8 errores de compilación E0599, todos en tests (ninguno en código fuente):

**STORY-011 (3 tests)**:
- `process_story_accepts_shared_state` (L1887) — llama a `process_story()` (async fn) sin `.await`
- `process_story_blocked_returns_early` (L1909) — llama a `process_story()` (async fn) sin `.await`
- `process_story_failed_returns_early` (L1930) — llama a `process_story()` (async fn) sin `.await`

**STORY-012 (5 errores en 4 tests)**:
- `run_real_with_terminal_stories_completes_in_one_iteration` (L2593) — llama a `run_real()` (async fn) sin `.await`
- `run_real_with_no_stories_completes_immediately` (L2633) — llama a `run_real()` (async fn) sin `.await`
- `run_real_with_draft_story_invokes_po_path` (L2680, L2683, 2 errores) — llama a `run_real()` (async fn) sin `.await`
- `run_real_shared_state_reflects_sequential_processing` (L2712) — llama a `run_real()` (async fn) sin `.await`

**Causa raíz**: los tests invocan funciones ahora `async fn` sin el runtime de tokio ni `.await`. Necesitan `#[tokio::test]` + `.await`.

**NO se corrigen los tests** — es trabajo del QA.

**NO se avanza a In Review** — el pipeline debe pasar el turno al QA para que corrija los tests.

---

## Decisión

Se mantiene el estado `Tests Ready`. El orquestador detectará los tests que no compilan y pasará el turno al QA automáticamente (transición Tests Ready → Tests Ready, QA corrige los tests).
