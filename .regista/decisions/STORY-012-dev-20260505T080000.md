# Decisión Dev — STORY-012 (2026-05-05)

## Resumen

Novena sesión de implementación de STORY-012. La implementación async ya está completada desde sesiones anteriores. No se realizan cambios en el código fuente.

## Verificación del código fuente

| Check | Resultado |
|-------|-----------|
| `cargo build` | ✅ Compila sin errores |
| `cargo clippy -- -D warnings` | ✅ Sin warnings |
| `cargo fmt --check` | ✅ Formato correcto |
| `cargo test` | ❌ 11 errores de compilación (solo en tests) |

## Errores de tests

### Tests síncronos que llaman funciones async sin `.await` (8 errores E0599)

| Línea | Test | Story | Llamada sin `.await` |
|-------|------|-------|----------------------|
| 1880 | `process_story_accepts_shared_state` | STORY-011 | `process_story(...)` → falta `.await` |
| 1902 | `process_story_blocked_returns_early` | STORY-011 | `process_story(...)` → falta `.await` |
| 1923 | `process_story_failed_returns_early` | STORY-011 | `process_story(...)` → falta `.await` |
| 2586 | `run_real_with_terminal_stories_completes_in_one_iteration` | STORY-012 | `run_real(...)` → falta `.await` |
| 2626 | `run_real_with_no_stories_completes_immediately` | STORY-012 | `run_real(...)` → falta `.await` |
| 2673 | `run_real_with_draft_story_invokes_po_path` | STORY-012 | `run_real(...)` → falta `.await` |
| 2676 | `run_real_with_draft_story_invokes_po_path` | STORY-012 | `result.unwrap()` → falta `.await` |
| 2705 | `run_real_shared_state_reflects_sequential_processing` | STORY-012 | `run_real(...)` → falta `.await` |

### Future no `Send` en `tokio::spawn` (3 errores)

| Error | Causa |
|-------|-------|
| `dyn Workflow` no es `Sync` | El trait `Workflow` referencia a `&dyn Workflow` que no es `Send` |
| `dyn AgentProvider` no es `Send` | El trait no requiere `Send`, `Box<dyn AgentProvider>` no es `Send` |
| `dyn AgentProvider` no es `Sync` | El trait no requiere `Sync`, `&dyn AgentProvider` no es `Sync` |

## Decisión

- **NO se corrigen los tests.** Es trabajo del QA migrar los tests a async (añadir `.await`, usar `#[tokio::test]`).
- **NO se avanza el estado a In Review.** Los CAs requieren que los tests pasen (CA6).
- El código fuente está listo y compila correctamente. Los tests son el único bloqueante.
