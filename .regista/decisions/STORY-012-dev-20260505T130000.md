# STORY-012 — Decimotercera sesión Dev (2026-05-05T13:00:00)

## Resumen

13ª sesión de implementación de STORY-012. La implementación async de `process_story()` y el loop
principal de `pipeline.rs` ya está completada desde la primera sesión (CA1-CA5 ✅).

## Estado de la implementación (sin cambios desde sesiones previas)

### CA1-CA5: completados ✅
- `process_story()` es `async fn` que invoca `invoke_with_retry(...).await`
- `run_real()` usa `process_story(...).await` secuencialmente
- `run_dry()` se mantiene síncrono (no invoca agentes)
- `run_hook()` usa `tokio::process::Command` vía `RUNTIME.block_on()`
- `snapshot()` y `rollback()` se invocan con `spawn_blocking`

### CA7: completado ✅
- `cargo build` — limpio, sin errores ni warnings
- `cargo clippy -- -D warnings` — limpio
- `cargo fmt --check` — limpio

### CA6: NO completado ❌ — 8 tests no compilan

## Tests que fallan (E0599 — llamadas a async fn sin .await)

Todos los errores están exclusivamente en código de test. El código fuente compila limpiamente.

### story011 (3 tests)
| # | Test | Línea | Error |
|---|------|-------|-------|
| 1 | `process_story_accepts_shared_state` | ~1887 | `process_story(...)` → Future, luego `.is_ok()` sin `.await` |
| 2 | `process_story_blocked_returns_early` | 1909 | `process_story(...)` → Future, luego `.is_ok()` sin `.await` |
| 3 | `process_story_failed_returns_early` | 1930 | `process_story(...)` → Future, luego `.is_ok()` sin `.await` |

### story012 (5 errores de compilación en 4 tests)
| # | Test | Línea | Error |
|---|------|-------|-------|
| 4 | `run_real_with_terminal_stories_completes_in_one_iteration` | 2593 | `run_real(...)` → Future, `.unwrap()` sin `.await` |
| 5 | `run_real_with_no_stories_completes_immediately` | 2633 | `run_real(...)` → Future, `.unwrap()` sin `.await` |
| 6 | `run_real_with_draft_story_invokes_po_path` (1ª llamada) | 2680 | `run_real(...)` → Future, `.is_ok()` sin `.await` |
| 7 | `run_real_with_draft_story_invokes_po_path` (2ª llamada) | 2683 | `.unwrap()` sin `.await` |
| 8 | `run_real_shared_state_reflects_sequential_processing` | 2712 | `run_real(...)` → Future, `.unwrap()` sin `.await` |

## Causa raíz

Las funciones `process_story()` y `run_real()` se migraron a `async fn` como parte de STORY-012.
Los tests correspondientes no se actualizaron para usar `#[tokio::test]` + `.await`.

## Acción requerida del QA

Convertir los 8 tests a async:
```rust
// Antes (no compila):
#[test]
fn test_ejemplo() {
    let result = process_story(...);
    assert!(result.is_ok());
}

// Después (compila):
#[tokio::test]
async fn test_ejemplo() {
    let result = process_story(...).await;
    assert!(result.is_ok());
}
```

## Decisión

NO se corrigen los tests — es responsabilidad del QA según el contrato de la máquina de estados
(transición Tests Ready → In Review solo si los tests compilan y pasan). NO se avanza a In Review.
El orquestador debe pasar el turno al QA automáticamente (transición Tests Ready → Tests Ready con QA).
