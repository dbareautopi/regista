# STORY-012 — Dev — 20260505T210000

## Resultado
⚠️ Implementación de código fuente completa, tests no compilan.

## Contexto
STORY-012 requiere migrar `process_story()` y el loop principal de `pipeline.rs` a async.
Las dependencias STORY-010 (`agent.rs` async) y STORY-011 (`Arc<RwLock<>>` para SharedState)
ya están implementadas.

## Verificación de CAs

### CAs completados (código fuente)

| CA | Estado | Detalle |
|----|--------|---------|
| CA1 | ✅ | `process_story()` es `async fn` — usa `invoke_with_retry(...).await` |
| CA2 | ✅ | `run_real()` usa `process_story(...).await` secuencialmente (sin `tokio::spawn`) |
| CA3 | ✅ | `run_dry()` es síncrono (no invoca agentes, no necesita tokio) |
| CA4 | ✅ | `run_hook()` usa `tokio::process::Command` con `RUNTIME.block_on` |
| CA5 | ✅ | `snapshot()`/`rollback()` se ejecutan con `spawn_blocking` desde `process_story()` |
| CA6 | ❌ | `cargo test --lib orchestrator` — no ejecutable (no hay lib target, solo binario) |
| CA7 | ✅ | `cargo build` compila sin warnings; `cargo clippy -- -D warnings` limpio; `cargo fmt --check` limpio |
| CA8 | ⚠️ | No verificado (dry-run requiere tests que no compilan) |

### Errores de compilación en tests (8 tests)

Los siguientes 8 tests NO compilan porque llaman a funciones `async fn` sin `.await`:

**STORY-011 (3 tests):**
- L1887: `process_story_accepts_shared_state` — `process_story(...)` sin `.await`
- L1909: `process_story_blocked_returns_early` — `process_story(...)` sin `.await`
- L1930: `process_story_failed_returns_early` — `process_story(...)` sin `.await`

**STORY-012 (5 tests):**
- L2593: `run_real_with_terminal_stories_completes_in_one_iteration` — `run_real(...)` sin `.await` + `.unwrap()`
- L2633: `run_real_with_no_stories_completes_immediately` — `run_real(...)` sin `.await` + `.unwrap()`
- L2680: `run_real_with_draft_story_invokes_po_path` — `result.is_ok()` sobre Future sin `.await`
- L2683: `run_real_with_draft_story_invokes_po_path` — `result.unwrap()` sobre Future sin `.await`
- L2712: `run_real_shared_state_reflects_sequential_processing` — `run_real(...)` sin `.await` + `.unwrap()`

Todos estos tests necesitan: `#[tokio::test]` + `.await` en las llamadas.

### Decisión

**NO se corrigen los tests.** Es trabajo del QA según el contrato de regista.
El orquestador detectará que los tests no pasan y pasará el turno al QA automáticamente.

El estado de la historia se mantiene en **Tests Ready**.
